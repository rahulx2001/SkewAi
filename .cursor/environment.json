{
  "name": "Skew AI",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "api",
      "command": "source .venv/bin/activate && DOMAIN_PACK=automotive_nhtsa uvicorn src.api.main:app --host 0.0.0.0 --port 8000",
      "description": "Skew AI FastAPI backend + agents. Serves the API and the built dashboard at http://localhost:8000/ui/ (health: /health)."
    }
  ],
  "ports": [
    {
      "name": "api",
      "port": 8000
    }
  ]
}
