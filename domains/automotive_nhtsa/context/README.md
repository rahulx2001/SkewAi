# Qubot context additions for automotive_nhtsa
#
# These would be merged with platform context at load time. There is no
# context_lint module in v2; the platform context is sufficient for the
# ask-data layer. This directory is reserved for domain-specific schema
# notes, semantics, and routing hints that a future Qubot extension would use.
#
# Currently empty — the platform context covers the canonical schema.
